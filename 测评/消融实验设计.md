# 消融实验设计（完整版）

## 一、目的

证明 PaperWise 的两个关键组件**确实有用，而非摆设**：

1. **记忆系统 → 主动推荐**（核心卖点：自动学习兴趣并推荐论文）；
2. **Harness 安全层**（安全防护是否真的拦得住攻击）。

遵循上一份 [消融分析.md](./消融分析.md) 里的铁律：

- 一次只动一个变量；
- 优先用**确定性指标**（不依赖 LLM 打分，避免同源 Judge 噪声）；
- 用**真实数据**（真实论文、真实攻击样本）。

## 二、消融 1：记忆系统对“主动推荐”的贡献

### 2.1 要证明什么

- 没有记忆 → 无法推荐（证明记忆是**必要条件**）；
- 仅靠用户手动声明 → 只能覆盖“明说过的方向”；
- 加入「论文信号」（从用户读过的论文自动学习主题）→ 能**自动发现声明之外的关联兴趣**，推荐更全。

### 2.2 变量（三个条件）

| 条件 | 记忆构成 | 说明 |
|------|---------|------|
| C0 无记忆 | 空 | 基线，期望“无推荐” |
| C1 仅声明 | 只有 `research_fields: "3D Gaussian Splatting"` | 用户只手动写了一个方向 |
| C2 完整 | 声明 + 三篇论文信号 | 当前系统实际形态 |

### 2.3 真实数据（手工构建）

- **用户声明兴趣**：`3D Gaussian Splatting`
- **论文信号**：三篇真实论文的自动提取主题（复用上一份测评解析出的主题）
  - 3DGS → `gaussian splatting / radiance field / anisotropic / ...`
  - LangSplat → `language field / SAM / CLIP / autoencoder / ...`
  - Feature 3DGS → `feature fields / SAM / CLIP-LSeg / distillation / ...`
- **候选论文池（真实 arXiv 论文）**：
  - 相关：3DGS、LangSplat、Feature 3DGS、Mip-Splatting、GaussianEditor、Segment Anything(SAM)
  - 无关：Attention Is All You Need、BERT、AlphaFold、ResNet、LLM Agents Survey

> 关键设计点：加入 **Segment Anything（SAM）** 作为“相关但不含 ‘Gaussian Splatting’ 字面”的论文。它只能靠“论文信号从 LangSplat 里学到 SAM 兴趣”才能被发现，用来区分 C1 和 C2。

### 2.4 指标

- **画像主题数**：C0=0 < C1=1 < C2（多个）；
- **推荐覆盖率**：返回的相关论文数量；
- **推荐相关度**：相关论文是否排在前、得分是否高于无关论文；
- **SAM 是否被命中**：C1 应漏掉、C2 应命中（核心区分指标）。

## 三、消融 2：Harness 安全层覆盖率审计

### 3.1 要证明什么

当前安全层对**真实攻击样本**的拦截覆盖率和误报率，量化“它到底拦住了什么、漏了什么”。

### 3.2 真实数据（手工构建）

- **攻击样本（24 条）**，分四类：
  - 提示注入（8 条）：ignore/disregard instructions、`<|im_start|>`、`[SYSTEM]`、DAN 等；
  - 危险命令（6 条）：rm -rf、sudo、curl|sh、`$(...)`、chmod、dd；
  - 路径越权（6 条）：`../../etc/passwd`、`C:\Windows\System32`、`.ssh/id_rsa`、`.aws/credentials` 等；
  - 密钥泄露（4 条）：`sk-` 长串。
- **良性样本（10 条）**：ls、read paper.md、summarize、grep 等。

### 3.3 指标

- **拦截率**：攻击样本被正确拦截的比例（越高越好，理想 100%）；
- **误报率**：良性样本被误拦的比例（越低越好，理想 0%）；
- **分类覆盖**：四类攻击各自的拦截情况，找出缺口。

## 四、为什么这两个消融可信

1. **完全确定性**：不调用 LLM 生成回答，只用规则/函数断言，结果可 100% 复现；
2. **真实数据**：论文和攻击样本都取自真实场景，不是“自己编一个刚好能过的题”；
3. **指标简单直接**：拦截率/误报率/命中率都是能直接解读的数字。

## 五、局限（诚实声明）

- 消融 1 通过 **stub 掉 arXiv 网络拉取**、用固定候选池来保证确定性，因此它证明的是“**画像 → 推荐的机制价值**”，而不是“真实联网检索的召回效果”。
- 消融 2 是“覆盖率审计”，不是严格的“开安全层 vs 关安全层”双跑对比（因为“关掉”等于没有任何防护，结论平凡；更有价值的是量化当前覆盖与缺口）。
- 这两项没有覆盖「RAG 是否提升问答正确率」「多 Agent 是否优于单 Agent」，后者需要 LLM 多次重复，噪声大，留作下一阶段。
